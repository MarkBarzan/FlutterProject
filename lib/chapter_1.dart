import 'dart:math';

void main() {

  // Mini-exercise 1
  // Print the value of 1 over the square root of 2. Confirm that it equals the sine of 45*
  //
  // double sin45 = sin(45 * pi / 180);
  // print(sin45);
  // double equalOfSin45 = 1 / sqrt(2);
  // print(equalOfSin45);
  // bool isEqual = sin45 == equalOfSin45;
  // print(isEqual);
  //
  //
  // Mini-exercise 2
  // Declare a constant of type int called myAge and set it to your age
  //
  // int myAge = 25;
  //
  // Mini-exercise 3
  // Declare a variable of type double called averageAge. Initially, set it to your own age.
  // Then, set it to the average of your age and that of your best friend.
  //
  // double averageAge = 25;
  // averageAge = (myAge + 29) / 2;
  // print(averageAge);
  //
  //
  // Mini-exercise 4
  //
  // Create a constant called testNumber and initialize it with whatever integer you want.
  // Next, create another constant called evenOdd and set it to equal testNumber modulo 2.
  // Now, change testNumber to various values and note what you observe.
  // const int testNumber = 5;
  // const double evenOdd = testNumber % 2;
  // print(evenOdd);
  //
  //
  // Challenge 1
  // Declare a constant int called myAge and set it equal to your age.
  // Declare an int variable called myDogs and set that to the equal of the number of dogs you own.
  // Then imagine you bought a new puppy and increment the number of dogs you own by one.
  //
  // const int c1MyAge = 25;
  // int myDogs = 0;
  // myDogs += 1;
  // print(myDogs);
  //
  //
  // Challenge 2
  // Given some code, modify it so the code compiles. Did you use var, int, const or final? Why?
  //
  // int age = 16;
  // print(age);
  // age = 30;
  // print(age);
  // Added int out of "var, int, const and final". Var works as well;
  // Const and final do not work due to already having a set value;
  //
  //
  // Challenge 3
  // Consider the following constants
  //
  // const int x = 46;
  // const int y = 10;
  //
  // Work out what each answer equals to when you add the following lines of code.
  //
  // const int answer1 = (x * 100) + y; // 4610
  // const int answer2 = (x * 100) + (y * 100); // 5600
  // const int answer3 = (x * 100) + (y / 10); // 4601
  // print(answer1);
  // print(answer2);
  // print(answer3);
  //
  //
  //
  // Challenge 4
  // Declare three constants, rating1, rating2, and rating3 of type double and assign them values.
  // Calculate the average rating and store the result in a const called averageRating.
  //
  // const double rating1 = 1;
  // const double rating2 = 3;
  // const double rating3 = 5;
  // const double averageRating = (rating1 + rating2 + rating3) / 3;
  // print(averageRating);
  //
  //
  // Challenge 5
  // Declare three constants a, b, and c of type double. Then calculate the two values of x in a quadratic equation.
  // Store the results in two variables root1 and root2.
  //
  // const double a = 1;
  // const double b = -2;
  // const double c = 1;
  // double x = (-b + sqrt((b * b) - (4 * a * c))) / (2 * a);
  //
  // print(x);
  //

}
